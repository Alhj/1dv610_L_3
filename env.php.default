<?php

class env
{
    private $fileName = // filte to snip

    private $userName = // the username to loggin with

    private $passWord = // the password to loggin with 

    public function setEnvData()
    {
        putenv("fileName=$this->fileName");
        putenv("userName=$this->userName");
        putenv("passWord=$this->passWord");
    }
}